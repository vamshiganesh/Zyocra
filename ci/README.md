# ci

Local-first CI placeholders. No paid runners or cloud secrets required for phase 1.

**Planned:** optional GitHub Actions for `forge test`, lint, and gas snapshots once contracts and circuits exist. Prefer commands that also run via `make test` / `make lint` / `make benchmark` on Ubuntu WSL.
