# quantization

Manual fixed-point quantization is a first-class benchmark axis.

**Planned:** float32 baseline, fixed-point export, scale-factor selection, overflow bounds, float vs field-compatible error analysis.

See `docs/architecture.md`.
