# ml-base stack (CPU-only, local-first).
# Install via: make install
#
# torch is installed separately from the official CPU wheel index in
# scripts/install.sh so WSL setups do not pull a CUDA stack.

onnx
onnxruntime
numpy
ezkl
